export function TaperCrew(area, finishes) {
    if (!area || !finishes || !finishes.wallTextures || !finishes.ceilingTextures || !finishes.corners) {
        return 0;
    }
    let total = 0;
    
    //logic here
    return total;
}

export function getTaperRate(bid, finishes) {
    //logic here
    return 0
}